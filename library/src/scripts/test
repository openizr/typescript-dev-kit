#!/bin/sh

command=$([ "$1" == "-w" ] && echo "watch" || echo "run")
NODE_ENV=test && ./node_modules/.bin/vitest "$command" --passWithNoTests --root=../../ --config=./node_modules/typescript-dev-kit/config/vite.config.js --coverage